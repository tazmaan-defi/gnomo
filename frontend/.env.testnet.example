# Testnet configuration for Vercel deployment
# Set these as Environment Variables in Vercel dashboard

# Test9 (stable, persisted state - recommended)
NEXT_PUBLIC_RPC_URL=https://rpc.test9.testnets.gno.land:443
NEXT_PUBLIC_CHAIN_ID=test9
NEXT_PUBLIC_CHAIN_NAME=Gno Test9

# Alternative: Staging (latest features, rolling updates)
# NEXT_PUBLIC_RPC_URL=https://rpc.gno.land:443
# NEXT_PUBLIC_CHAIN_ID=staging
# NEXT_PUBLIC_CHAIN_NAME=Gno Staging

# Contract paths (update these after deployment!)
NEXT_PUBLIC_GNOMO_PKG_PATH=gno.land/r/gnomo/dex
NEXT_PUBLIC_CLMM_PKG_PATH=gno.land/r/gnomo/clmm
